# Google Maps JS API browser key — powers the Search page basemap.
# Without it, search still works; the map shows a quiet placeholder.
# Restrict the key to your domains in the Google Cloud console.
VITE_GOOGLE_MAPS_API_KEY=
