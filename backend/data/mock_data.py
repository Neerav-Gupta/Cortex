"""Seed listing data.

Empty by default — Cortex now analyzes whatever addresses are added through
the UI, using live Census/FRED data plus the price details the homeowner
provides.
"""

LISTINGS: dict[str, dict] = {}
